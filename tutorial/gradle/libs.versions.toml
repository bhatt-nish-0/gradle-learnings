# This file was generated by the Gradle 'init' task.
# https://docs.gradle.org/current/userguide/platforms.html#sub::toml-dependencies-format

[versions]
guava = "33.3.1-jre"
junit-jupiter = "5.11.1"
# dependencies in the build.gradle can reference the versions here.
[libraries]
guava = { module = "com.google.guava:guava", version.ref = "guava" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit-jupiter" }

# com.google.guava:guava version 33.3.1-jre
# com.google.guava is the group
# guava is the name
# 33.3.1-jre is the version
# what is a transitive dependency?
# a transitive dependency is one which is a dependency of a dependency
# so now, we can view the project dependencies using the following

# ./gradlew.bat :app:dependencies
#PS C:\Users\nisch\Downloads\x\gradle-learnings\tutorial> ./gradlew.bat :app:dependencies
#Calculating task graph as no cached configuration is available for tasks: :app:dependencies
#thsi is init
#
#> Task :app:dependencies
#
#------------------------------------------------------------
#Project ':app'
#------------------------------------------------------------
#
#annotationProcessor - Annotation processors and their dependencies for source set 'main'.
#No dependencies
#
#compileClasspath - Compile classpath for source set 'main'.
#\--- com.google.guava:guava:33.3.1-jre
#+--- com.google.guava:failureaccess:1.0.2  --> this is a transitive dependency
#+--- com.google.guava:listenablefuture:9999.0-empty-to-avoid-conflict-with-guava
#+--- com.google.code.findbugs:jsr305:3.0.2
#+--- org.checkerframework:checker-qual:3.43.0
#+--- com.google.errorprone:error_prone_annotations:2.28.0
#\--- com.google.j2objc:j2objc-annotations:3.0.0
